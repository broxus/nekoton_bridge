targets:
  $default:
    sources:
      - lib/**
      # Note that it is important to include these in the default target.
      - pubspec.*
      - $package$
    builders:
      reflectable:
        generate_for:
          - lib/storage.dart
        options:
          formatted: true
